# Netlify Environment Variables
# Copy this to Netlify UI: Site Settings → Environment Variables

# Backend API URL (REQUIRED)
# For production: Your deployed backend URL on Render
# For staging/preview: Can use staging backend or same as production
REACT_APP_API_URL=https://your-backend.onrender.com

# Environment identifier (optional)
REACT_APP_ENV=production

# Google Analytics (optional)
# REACT_APP_GA_TRACKING_ID=UA-XXXXXXXXX-X

# Sentry Error Tracking (optional)
# REACT_APP_SENTRY_DSN=https://xxxx@sentry.io/xxxxx

# Feature Flags (optional)
# REACT_APP_ENABLE_ANALYTICS=true
# REACT_APP_ENABLE_DARK_MODE=true

# ============================================
# HOW TO SET UP ENVIRONMENT VARIABLES IN NETLIFY
# ============================================
#
# 1. Via Netlify UI (Recommended for production):
#    a. Go to your site in Netlify Dashboard
#    b. Navigate to: Site Settings → Environment Variables
#    c. Click "Add a variable"
#    d. Add each variable from this file
#    e. Choose scope: "All deploys" or specific contexts
#
# 2. Via Netlify CLI (for local testing):
#    a. Install CLI: npm install -g netlify-cli
#    b. Login: netlify login
#    c. Link site: netlify link
#    d. Set variables: netlify env:set REACT_APP_API_URL "https://your-backend.onrender.com"
#
# 3. Via netlify.toml (for non-sensitive values):
#    [build.environment]
#      REACT_APP_ENV = "production"
#
# ============================================
# IMPORTANT NOTES
# ============================================
#
# - Only variables starting with REACT_APP_ are available in the frontend
# - Environment variables are embedded at BUILD TIME, not runtime
# - Changing variables requires a new build/deployment
# - Sensitive values (API keys) should ONLY be set via Netlify UI (marked as "Secret")
# - Use different values for production vs deploy-preview contexts
#
# ============================================
# CONTEXT-SPECIFIC VARIABLES (Advanced)
# ============================================
#
# You can set different values for different contexts in Netlify UI:
#
# Production (main branch):
#   REACT_APP_API_URL = https://api.heritage.com
#   REACT_APP_ENV = production
#
# Deploy Previews (PR builds):
#   REACT_APP_API_URL = https://staging-api.heritage.com
#   REACT_APP_ENV = staging
#
# Branch deploys:
#   REACT_APP_API_URL = https://dev-api.heritage.com
#   REACT_APP_ENV = development
